#include<iostream>
#include<cmath>
#include<cstring>
using namespace std;
int main() {
    if(true)
    {
        cout<<"true means 1 in binary ";
    }
    else
    {
        cout<<"if if(false) there then this statement will be printed";
    }
    return 0;
}