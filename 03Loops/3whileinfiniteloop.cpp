#include<iostream>
#include<cmath>
#include<cstring>
using namespace std;
int main() {
int i=1;
while(i<10)            // after performing infinite loop restart the vs code for running next program
{
    cout<<"i    "<<i<<endl; //as i+=1 not there so i=1 which is always less than 10
}
return 0;
}